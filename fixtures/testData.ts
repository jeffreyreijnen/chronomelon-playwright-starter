export type CustomerInfo = {
  firstName: string;
  lastName: string;
  postalCode: string;
};

export const customer: CustomerInfo = {
  firstName: 'Jane',
  lastName: 'Doe',
  postalCode: '12345',
};

export default { customer };


